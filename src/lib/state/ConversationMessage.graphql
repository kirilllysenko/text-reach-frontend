query ConversationMessage($id: Ulid!) {
  message(id: $id) {
    campaign {
      id
      name
    }
    conversation {
      id
    }
    createdAt
    direction
    id
    media {
      contentType
      sizeBytes
      url
    }
    receivedAt
    sentAt
    status
    text
  }
}
