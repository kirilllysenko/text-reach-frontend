query CampaignFormContactGroups @cache(policy: NetworkOnly) {
  contactGroups(first: 300, sortBy: [{ name: { direction: ASC } }]) {
    edges {
      node {
        contactCount
        id
        name
      }
    }
  }
}
