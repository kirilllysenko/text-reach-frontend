query ContactFormEditQuery($id: Ulid!) @cache(policy: NetworkOnly) {
  contact(id: $id) {
    birthday
    contactGroups {
      id
      name
    }
    customFields {
      customField {
        id
      }
      value
    }
    email
    firstName
    lastName
    notes
    phoneNumber
  }
}
