query TenDlcCampaigns {
  tenDlcCampaigns(first: 50, sortBy: [{ createdAt: { direction: DESC } }]) {
    edges {
      node {
        id
        description
        providerStatus
        usecase
      }
    }
    totalCount
  }
}
