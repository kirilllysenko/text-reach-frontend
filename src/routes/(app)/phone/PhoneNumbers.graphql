query PhoneNumbers {
  tenantPhones(first: 100, sortBy: [{ phoneNumber: { direction: ASC } }]) {
    edges {
      node {
        id
        phoneNumber
        phoneType
      }
    }
    totalCount
  }
}
