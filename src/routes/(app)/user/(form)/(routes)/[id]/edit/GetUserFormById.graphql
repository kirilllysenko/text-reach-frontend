query UserFormEditQuery($id: Ulid!) @cache(policy: NetworkOnly) {
  users(filter: { id: { in: [$id] } }, first: 1) {
    edges {
      node {
        email
        id
        name
        role
      }
    }
  }
}
